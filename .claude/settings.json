{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const f=JSON.parse(process.env.CLAUDE_TOOL_INPUT||'{}'); const p=(f.file_path||'').replace(/\\\\\\\\/g,'/'); if(p.endsWith('.env')||p.includes('.env.')){process.stdout.write('BLOCK: .env files contain API secrets and must not be edited');process.exit(2)}\""
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const f=JSON.parse(process.env.CLAUDE_TOOL_INPUT||'{}'); const p=(f.file_path||'').replace(/\\\\\\\\/g,'/'); if(p.endsWith('package-lock.json')){process.stdout.write('BLOCK: Do not edit lock files directly — use npm install');process.exit(2)}\""
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const f=JSON.parse(process.env.CLAUDE_TOOL_INPUT||'{}'); const p=(f.file_path||'').replace(/\\\\\\\\/g,'/'); if(p.endsWith('products.json')&&!p.includes('custom-products')){process.stdout.write('BLOCK: products.json is auto-generated by Printify sync — run npm run sync instead');process.exit(2)}\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const f=JSON.parse(process.env.CLAUDE_TOOL_INPUT||'{}'); const p=(f.file_path||'').replace(/\\\\\\\\/g,'/'); if(p.endsWith('index.html')){const h=require('fs').readFileSync(p,'utf8'); const issues=[]; if(!h.includes('</html>'))issues.push('missing </html>'); if(!h.includes('</head>'))issues.push('missing </head>'); if(!h.includes('</body>'))issues.push('missing </body>'); if(issues.length){process.stdout.write('WARN: index.html structural issues: '+issues.join(', '));process.exit(1)}}\""
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Read",
      "Glob",
      "Grep",
      "Bash(npm run sync:*)",
      "Bash(npm install)",
      "Bash(npm test:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(gh *)"
    ]
  }
}
